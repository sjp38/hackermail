- Cleanup code
- Monitoring --pisearch updates
- Monitoring tagged threads
- Make list's --collapse by default
  - Once interactive list supports thread listing, --collapse is better
- Handle encoding of git-fetched mail
  - mail fetched from public-inbox via curl is handled well, while that from
    git is not
  - e.g., 20240603161530.80789-1-yskelg@gmail.com
- Document interactive list/viewer
- Support more inputs from 'hkml open'
  - message id
  - stdin
- Interactive viewer imporovements
  - Implement keyword search/highlight
  - Handle 'git' failure-causing screen corruption
  - Further check if git id is correct git id (e.g., do git log and check
    exception)
  - Support other features
    - exporting
    - writing
  - Support refreshing list
